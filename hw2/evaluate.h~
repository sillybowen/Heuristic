#ifndef evaluate_h_
#define evaluate_h_
#include <iostream>
#include "basic_geo.h"
#include <vector>
using namespace std;


class Evaluate {
 private:
  vector<point> points;  
 public:
  Evaluate(vector<point> p);
  double evalueate(vector<int> seq);
}
#endif
